import '@testing-library/jest-dom';

/**
 * TODO: Mobile Responsiveness Issues to be addressed:
 * 1. Horizontal Overflow: Grill (420px) and Bottom Row exceed mobile width.
 * 2. Drag & Drop: HTML5 Drag/Drop API is not touch-compatible.
 * 3. Viewport: h-screen (100vh) clipping on mobile browsers.
 * 4. Touch Targets: Small buttons in debug/UI panels.
 * 5. Layout: Missing responsive breakpoints for portrait/landscape.
 */
